export async function resetDaily() {
  // Cigarettes log is per-date in DB, so no explicit reset is needed.
  // Add other daily resets here if needed in the future.
  console.log('[resetDaily] Completed daily reset routine.')
}
